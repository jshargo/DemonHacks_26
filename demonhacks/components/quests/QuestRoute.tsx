// Placeholder — quests aren't in the real schema
// This component can be repurposed for event route visualization

import { View, Text, StyleSheet } from 'react-native';

export default function QuestRoute() {
  return (
    <View style={styles.container}>
      <Text>Route visualization — TODO</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { padding: 8 },
});
