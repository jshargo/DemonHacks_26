// Placeholder — quests aren't in the real schema

import { View, Text, StyleSheet } from 'react-native';

export default function QuestStopItem() {
  return (
    <View style={styles.container}>
      <Text>Stop item — TODO</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { padding: 8 },
});
