// Placeholder — collections picker is not needed with flat saved_items.
// Kept for import compatibility. Can be removed if unused.

import { View } from 'react-native';

export default function AddToCollection() {
  return <View />;
}
