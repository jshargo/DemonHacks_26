// Placeholder — collection cards are not needed with flat saved_items.
// Kept for import compatibility. Can be removed if unused.

import { View } from 'react-native';

export default function CollectionCard() {
  return <View />;
}
